// Importamos la clase.
const MissionCommander = require('./app/missionComander')

// Declaramos nuestros objetos.
const orlando = new MissionCommander('Orlando')
console.log(orlando.name)

const carlo = new MissionCommander('Carlo')
console.log(carlo.name)

const miguel = new  MissionCommander('Miguel')
console.log(miguel.name)

